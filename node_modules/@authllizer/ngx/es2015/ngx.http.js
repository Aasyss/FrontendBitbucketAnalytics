import { BaseHttpClient } from '@authllizer/core';
export class NgxHttpClient extends BaseHttpClient {
    constructor(httpClient) {
        super(httpClient);
    }
    request(url, options) {
        let { method, data, params, headers, withCredentials } = options;
        return this._client.request(method, url, {
            body: data,
            params,
            headers,
            withCredentials
        }).toPromise();
    }
}
//# sourceMappingURL=ngx.http.js.map
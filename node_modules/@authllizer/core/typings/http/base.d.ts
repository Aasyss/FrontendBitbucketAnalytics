import { Directory } from '../interface';
import { IHttpClient, IHttpRequestOptions } from './http';
export declare abstract class BaseHttpClient implements IHttpClient {
    static extendUrlQuery(url: string, params: Directory<any>): string;
    protected _client: any;
    constructor(client?: any);
    abstract request<T>(url: string, options: IHttpRequestOptions): Promise<T>;
}

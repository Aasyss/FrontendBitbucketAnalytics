import { IToken } from './token';
export interface IJWTPayload {
    iss?: string;
    sub?: string;
    aud?: string;
    exp?: number;
    nbf?: string;
    iat?: number;
    jti?: string;
    [key: string]: any;
}
export declare class JWT implements IToken {
    static JWT_RX: RegExp;
    static parse(token: string): IJWTPayload;
    readonly expire: Date;
    private _token;
    private _payload;
    protected headerPrefix: string;
    constructor(token: string | JWT);
    getPayload(): IJWTPayload;
    isExpired(): boolean;
    isValid(): boolean;
    toHeader(): string;
    toString(): string;
    toJSON(): IJWTPayload;
}

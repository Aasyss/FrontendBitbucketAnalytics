export interface Listener<T> {
    (value?: T): void;
}
export default class EventEmitter<T> {
    protected _listeners: Listener<T>[];
    constructor(...listeners: Listener<T>[]);
    emit(value?: T): this;
    on(...listeners: Listener<T>[]): this;
    remove(...listeners: Listener<T>[]): this;
}

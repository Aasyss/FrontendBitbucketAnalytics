import { MemoryStorage } from './memory';
import { IStorage } from './storage';
import { IToken } from '../tokens/token';
export declare class SessionStorage extends MemoryStorage implements IStorage {
    getToken(): string;
    setToken(token: IToken): void;
    removeToken(): void;
}
